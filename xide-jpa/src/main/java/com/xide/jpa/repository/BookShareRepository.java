package com.xide.jpa.repository;

import com.xide.jpa.entity.BookShare;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BookShareRepository extends JpaRepository<BookShare, String> {
}